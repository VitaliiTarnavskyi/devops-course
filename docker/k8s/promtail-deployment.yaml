apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml -o k8s/
    kompose.version: 1.24.0 (4a2a0458)
  creationTimestamp: null
  labels:
    io.kompose.service: promtail
  name: promtail
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: promtail
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.yml -o k8s/
        kompose.version: 1.24.0 (4a2a0458)
      creationTimestamp: null
      labels:
        io.kompose.service: promtail
    spec:
      containers:
        - args:
            - -config.file=/etc/promtail/promtail.yml
          image: grafana/promtail:2.9.4
          name: promtail
          resources: {}
          volumeMounts:
            - mountPath: /var/log
              name: promtail-claim0
            - mountPath: /etc/machine-id
              name: promtail-claim1
            - mountPath: /etc/promtail/promtail.yml
              name: promtail-claim2
            - mountPath: /var/lib/docker/containers
              name: promtail-claim3
              readOnly: true
            - mountPath: /var/run/docker.sock
              name: promtail-claim4
      restartPolicy: Always
      volumes:
        - name: promtail-claim0
          persistentVolumeClaim:
            claimName: promtail-claim0
        - name: promtail-claim1
          persistentVolumeClaim:
            claimName: promtail-claim1
        - name: promtail-claim2
          persistentVolumeClaim:
            claimName: promtail-claim2
        - name: promtail-claim3
          persistentVolumeClaim:
            claimName: promtail-claim3
            readOnly: true
        - name: promtail-claim4
          persistentVolumeClaim:
            claimName: promtail-claim4
status: {}
